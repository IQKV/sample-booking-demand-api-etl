spring :
  application :
    name : edge-service
  cloud :
    compatibility-verifier :
      enabled: false
application-configuration :
  service-discovery :
    services :
      dashboard-service : http://dashboard-service
