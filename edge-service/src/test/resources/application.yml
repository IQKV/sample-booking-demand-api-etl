spring :
  application :
    name : edge-service
  cloud :
    compatibility-verifier :
      enabled: false
expertness :
  service-discovery :
    services :
      dashboard-service : http://dashboard-service
